import { ArchiveView } from '../views/ArchiveView';

/**
 * Geo-Archive Page
 * Gallery & Comparison view for all provinces
 * Features: Filter by region, cost, safety | Sort | Compare up to 3 provinces
 */
export const GeoArchivePage = () => {
  return <ArchiveView />;
};
