const fs = require('fs'); const path = require('path'); const d = 'src/renderer/src/pages/ProvinceTactical/tabs'; fs.readdirSync(d).filter(f => f.endsWith('.tsx')).forEach(f => { let p = path.join(d, f); let c = fs.readFileSync(p, 'utf8'); c = c.replace(/<\/ContentCard>/g, '</Helpers.ContentCard>'); c = c.replace(/<(CollapsibleSection|ExternalLinkCard|LinkCard) /g, '<Helpers.\ '); c = c.replace(/<\/(CollapsibleSection|ExternalLinkCard|LinkCard)>/g, '</Helpers.\>'); fs.writeFileSync(p, c, 'utf8'); console.log('fixed', f); });
